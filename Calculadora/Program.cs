using Calculadora.Models;

//Instanciando a classe
Calculo calc = new Calculo();  

//Chamando os metodos
    calc.Somar(10, 20);
    calc.Subtrair(10, 20);
    calc.Multiplicar(10, 20);
    calc.Dividir(10, 20);

    calc.Potencia(3,3);