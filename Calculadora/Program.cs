using Calculadora.Models;

//Instanciando a classe
Calculo calc = new Calculo();  

//Chamando os metodos
    // calc.Somar(10, 20);
    // calc.Subtrair(10, 20);
    // calc.Multiplicar(10, 20);
    // calc.Dividir(10, 20);

    // calc.Potencia(3,3);

    // calc.Seno(30);
    // calc.Coseno(30);
    // calc.Tangente(30);

    calc.RaizQuadrada(9);